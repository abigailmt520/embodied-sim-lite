# CONTRIBUTING — 参与 Embodied-SimLite

感谢关注。本仓同时是一门在开课程的**冻结依赖**与已录用论文的**复现工件**，自 `v1.1.1` 起处于**冻结基线维护态**，所以贡献规则比一般项目严。规则只为一件事：任何改动都不能悄悄改变课程已钉版本与论文复现结果。

## 1 先读三份文件

- `ITERATION.md`：迭代纪律（加法＋开关、三道门、tag only、契约语义化版本、冻结物变更程序、性能预算、发布线）。
- `FROZEN-CI.md`：三道 CI 门的定义、子集与容差。
- `docs/teaching_api.md`：教学接口契约（现状快照）。

## 2 本仓现为冻结基线维护态（自 v1.1.1 起）

1. **只接受 hotfix**：修复使冻结基线无法按其既有文档复现或运行的缺陷（依赖失效、平台差异、文档错误致不可复现）；**不接受新功能与行为改进**。
2. 流程见 `FROZEN-CI.md` §7：自受影响的冻结 tag 或 `master` 切 `maint/<日期>-<事由>` 分支 → 本地 `python3 tools/ready_check.py --all` 三门全绿 → PR 上 CI 三门全绿（`master` 受分支保护）→ 快进合入 → 补丁附注 tag。
3. 仍然适用：开关关闭态下既有入口的产物与响应须与改动前逐字节一致（`ITERATION.md` §2）；触及冻结物（`golden/`、`artifacts/HASHES.lock`、冻结子集、`course_manifest.yaml`、契约版本号、`FROZEN-CI.md` 既有条目）须走 `ITERATION.md` §6 冻结物变更程序，**不接受直接修改**；改教学接口契约按 §5 判定版本并附迁移说明。
4. 每个对外可见的变更在 `CHANGELOG.md` 记一条，**必须带「教学影响」字段**。

## 3 红线

- **凭据不入代码**：一切凭据只经环境变量注入；推送前经 `tools/audit_secrets.py` 全树审计（维护者经 `tools/publish.sh` 推送）。
- **不放个人信息**：学生姓名、学号、成绩等一律不入仓；审计包 uid 只用匿名代号。
- **攻击语料隔离**：S5 类样本在代码注释、文档、提交信息、Issue 中只引 benchmark id，不复述文本。
- **不放未发表论文内容**：论文正文、审稿意见、未公开的研究设计不入本仓。
- `artifacts/` 只增不改；课程与论文只认冻结 tag。

## 4 AI 辅助生成内容声明

- 本仓部分代码与文档在 AI 编程助手辅助下编写；相关提交通常在提交信息中以 `Co-Authored-By` 行标注。
- AI 辅助产出与人工编写适用**同一套**纪律：须过三道 CI 门、由维护者审阅后合并；许可证（Apache-2.0）同样适用。
- 维护者对合入内容负责。若发现疑似第三方受版权保护的内容或许可证不兼容的片段，请提 Issue 指明位置，维护者核实后处理。
- 贡献者提交 AI 辅助产出时，请在 PR 描述中说明，并确认你有权以 Apache-2.0 提交该内容。

## 5 沟通

- 缺陷与建议：提 Issue，附复现步骤与 `ready_check` 输出。
- 行为准则：见 `CODE_OF_CONDUCT.md`。
