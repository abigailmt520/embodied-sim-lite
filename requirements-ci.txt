# CI 三道门锁定依赖（CSO-028-R1 裁定②）：与基准机 Mac14,9（golden/course-2026A/RECORD.json 指纹）逐版本一致，
# 消除 matplotlib / torch 版本差异带来的 golden 假漂移。升级任一版本＝重录 golden 并写入 RECORD.json history。
# torch 须先自 CPU 索引安装：pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cpu
numpy==2.4.6
torch==2.12.1
stable-baselines3==2.9.0
gymnasium==1.3.0
fastapi==0.137.1
uvicorn==0.49.0
websockets==16.0
matplotlib==3.11.0
pillow==12.2.0
pyyaml==6.0.3
