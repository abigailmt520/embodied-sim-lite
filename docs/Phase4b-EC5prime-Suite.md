# Phase4b 详情：EC5'（物理内真值-vs-地图）+ 常驻三层审计套件 —— 判据分离收尾

> 分支 `dev/stage6-ec5prime`（off Phase4 `9f188b1`）。master `7b54625` 冻结零改动；所有已有 .pth 不覆盖。
> **纯审计层（env 未改）**。数字真实运行（INV-E）；带行号（INV-B）。

---

## 0. 命题
Phase4 已证真耦合（场景 B）存在，并诚实指出：场景 A（agent 真穿墙）是 **EC5 实现缺口冒充耦合**
（现有 EC5 信任账本 penetration 字段，幽灵墙不在碰撞系统→报 0 而漏）。本任务**补上缺口**，让
「真耦合」与「审计缺口」在**代码层彻底分开**：①实现 EC5'（物理内几何重算）；②固化 joint_audit 进常驻套件。

---

## 1. ① EC5'（物理内「真值-vs-声称地图几何」重算）
- 实现：`joint_audit.ec5_prime(truth_traj, walls, radius)`（`joint_audit.py:60`）——**不信任账本 penetration**，
  直接用**真值位置**对照**声称全地图** MAZE_WALLS 几何独立判穿透。纯物理内（真值与地图皆物理侧），不需上报通道。
- 与既有 EC5 的区别：EC5 读账本 `penetration`（碰撞系统对 active_walls 算，幽灵墙被剔除→0）；
  EC5' 对**声称全地图**重算（含被剔除的幽灵墙）→ 抓 EC5 漏的真值真穿墙。

## 2. ② 常驻三层审计套件（`audit_suite.py`）
把各层固化为并列常驻套件 `run_suite`（`audit_suite.py:34`）：
- **契约层（report 自洽）**：C1 真分叉 / C2 帧序单调 / C3 断流即冻结 / CI 互信息泄漏。
- **物理层（physics 自洽）**：EC1 能量预算 / EC2 无凭空能量 / EC3 执行器上限 / EC4 碰撞非负 /
  EC5 非穿透(账本) / **EC5' 真值-vs-地图(物理内几何重算)**。
- **联合层（report×physics 跨态）**：JOINT odom-vs-声称地图（`joint_report_vs_map` `:73`）。
- 耦合判据 `coupling_label`（`:87`）：EC5' 红 ⇒ PHYSICS_INTERNAL（物理内单层可抓，非耦合）；
  EC5' 绿 ∧ JOINT 红 ⇒ TRUE_COUPLING（唯联合可抓）。

---

## 3. 验收（真实运行，`run_coupling_test.py`）

### 3.1 场景 A 字面穿墙幽灵（真值真穿墙）→ **EC5' 单层抓 = 非耦合**
| 层 | 结果 |
|---|---|
| 契约层 C1/C2/C3+CI | 🟢 全过 |
| 物理层 EC1-EC5 | 🟢（账本 penetration=0）|
| **物理层 EC5'** | **🔴**（真值落墙内 18 帧，最深穿透 0.200 m @帧20）|
| 联合层 JOINT | 🔴（odom 也穿墙）|
| **判定** | **PHYSICS_INTERNAL** |
→ **坐实**：场景 A 由**物理层 EC5' 单层独力判红**，不需 joint。「场景 A=单层缺口（已补 EC5'）、非耦合」。

### 3.2 场景 B 真耦合变体（真值诚实、odom 伪造穿墙）→ **唯 joint 抓 = 真耦合**
| 层 | 结果 |
|---|---|
| 契约层 C1/C2/C3+CI | 🟢 全过 |
| 物理层 EC1-EC5 | 🟢 |
| **物理层 EC5'** | **🟢**（真值合法、未替 joint 充数）|
| 联合层 JOINT | 🔴（odom 落墙内 12 帧 @帧13）|
| **判定** | **TRUE_COUPLING** |
→ **坐实**：物理层(含 EC5')🟢 + 契约层 🟢 + **EC5' 绿** + 唯 JOINT 红 → 真耦合唯联合可抓。

---

## 4. 🔴 诚实判据 1-3 的结论（全部满足）
1. **EC5' 零误报**：30 回合健康 maze **0/30 误报**（真值推出后残余穿透 <1e-3 < 判红下限）；
   clean/CF-1/CF-3 真值合法 → EC5' 🟢；CF-2/幽灵墙 真值真留墙内 → EC5' 🔴（**真违反、非误报**）。**未调参硬压。**
2. **EC5' 未替 joint 对场景 B 充数**：场景 B 真值合法 → **EC5' 🟢**（若 EC5' 红则说明场景 B 真值非法、非真耦合）。
   实测 EC5' 绿 → 场景 B 仍是「唯 joint 抓」的真耦合。
3. **判据分离干净**：场景 A=EC5'(物理内)单层抓（非耦合）、场景 B=唯 joint(跨态)抓（真耦合）——
   两者判据分离、各司其职。

无回归：碰撞 CF 3/3、能量 P 5/5、契约 C1-C3、泄漏 CI 3/3 均通过；env 未改（EC5' 纯审计层）。

---

## 5. 待办 / 范围外（INV-C）
- joint_audit/EC5' 接入 inference_server 运行时流（real-time 联合监控）；未做。
- 未接 MuJoCo；未碰契约 C1-3/CI、物理 EC1-5 既有判据；未做 F3 动态障碍；未做 q 类对账。
